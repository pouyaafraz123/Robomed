import React from "react";
import styled from "styled-components";

const Back = styled.div`
  background-image: linear-gradient(to right, var(--gradient-start), rgba(225, 188, 22, 0.2) 80%, var(--gradient-start));
  border: 0;
`;


export default Back;